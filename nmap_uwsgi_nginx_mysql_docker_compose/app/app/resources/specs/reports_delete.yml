Delete report
---
tags:
  - report
produces:
  - application/json
parameters:
  - name: scan_id
    in: path
    description: scan id
    required: true
responses:
  200:
    schema:
      id: delete_report_ok
      properties:
        message:
          type: string
          description: error message
          example: 'ok.'